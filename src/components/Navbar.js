import React, { Component } from 'react'
import { Colors } from '@blueprintjs/core'

export default class Navbar extends Component {
  render() {
    return (
      <div style={{ backgroundColor: Colors.WHITE, height: '90vh' }}>
        Navbar
      </div>
    )
  }
}
